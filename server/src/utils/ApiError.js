// Operational error with an HTTP status. Thrown anywhere; formatted by errorHandler.
export default class ApiError extends Error {
  constructor(status, message, details) {
    super(message);
    this.name = 'ApiError';
    this.status = status;
    this.details = details;
  }
}
